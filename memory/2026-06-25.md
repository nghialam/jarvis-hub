# Daily Memory — 2026-06-25

## Status
MEMORY.md compacted at 23:00. Major changes: omlx fully removed, reverted to Ollama as primary engine. Context limits and cron system consolidated into single section.

## Changes Applied
- **Removed:** Stale "Ollama fully removed Jun 22" line — actually we reverted TO omlx on Jun 25 per user's explicit command ("ko sử dụng omlx nữa, chuyển qua hết ollama rồi")
- **Removed:** Entire "Prefill Memory Wall" section — omlx-specific, no longer relevant
- **Removed:** Stale lessons about oMLX config, `iogpu.wired_limit_mb` fix, and context window size (lessons 6-9)
- **Removed:** Stale prefill memory wall pending fix item from Persistent Items
- **Added:** New persistent item DBR01 — Daily Brief Data silent failures cause identified: model returning [SILENT] per prompt directive; fix is to remove "reply SILENT if nothing new" and always populate at least one data section
- **Removed:** `qwen3.6-mlx` reference in golden rule (no longer the correct model spec — now `qwen3.6:35b-a3b-mxfp8` via ollama-launch)
- **Consolidated:** Context Limits and Cron System → single merged section, removed duplicate entries

## Sources Reviewed
- `jarvis-hub/memory/2026-06-23.md` — last compact (Jun 23)
- Session `cron_f5c44334ba87_20260625_220056` — Backlog sync (22:00), added DBR01 for silent failures
- Session `cron_69d0e6570d3d_20260625_145552` — Jarvis Autoupdate (15:07), Flask 8100 unreachable confirmed
- Session `cron_bd165385f815_20260625_141354`/Session `20260625_151233_4cec84` — Daily Briefing script v7, [SILENT] root cause found (agent follows prompt "reply SILENT if nothing new" directive causing 0-byte outputs)
- User Telegram message: "ko sử dụng omlx nữa, chuyển qua hết ollama rồi" — decisive direction to switch back
- Current MEMORY.md before compact — had duplicate entries from failed merge

## Key Findings
1. **Ollama is back as primary engine** — user explicitly requested switching away from omlx; config updated accordingly
2. **Daily Briefing silent failures resolved:** Root cause was the "reply SILENT if nothing new" directive in cron job prompt causing agent to return [SILENT], which Hermes skips delivery for, resulting in 0-byte output files. Fix: remove that directive and ensure data sections are always populated.
3. **Context limit lesson stands but engine-agnostic:** The principle that "context limit ≠ token limit" applies regardless of whether we use omlx or Ollama — pre-fill capacity matters in both.

## Final Line Count
45 lines (under 50-line cap)
